# Devlog 2026-07-26

## Đã làm

### User Guide — mục AI機能
- `docs/USER_GUIDE.md`: thêm section 2 mới "AIでできること" (đẩy 基本機能/詳細仕様/デモシナリオ xuống 3/4/5), giải thích 4 tính năng AI theo format trước đây/bây giờ dễ hiểu.
- Artifact HTML (`user-guide.html`, republish cùng URL): thêm section tương ứng, CSS mới `.ai-feature-grid`/`.ai-num`/`dl.ba`.

### Tách repo git riêng + public qua ngrok
- Cài `git` (2.55.0) và `ngrok` (3.39.10) qua winget — máy trước đó không có sẵn cả hai trong PATH của shell.
- Tạo thư mục `f:\Prj\CaseMngmt-demo` — bản copy sạch của working tree (loại trừ `.git`/`bin`/`obj`/`node_modules`/`build`/`UploadedFiles`), sanitize `appsettings.json` (xoá AWS key + JWT secret thật, giữ nguyên `f:\Prj\CaseMngmt` gốc không đổi), `git init` + push 1 commit sạch lên **https://github.com/jinaomi/order-platform-dx** (repo mới, không mang theo lịch sử leak secret của repo `case-management` cũ).
- Public app qua ngrok: sửa `frontend/src/api/axios.js` (đổi `||` thành check `!== undefined` để hỗ trợ `REACT_APP_BASE_URL` rỗng), thêm `frontend/.env.production.local` (`REACT_APP_BASE_URL=` rỗng — dùng file thay vì set qua shell vì Windows/cmd.exe tự xoá env var rỗng), build frontend production và mirror vào `backend/CaseMngmt.Server/wwwroot/`, chạy backend với `--no-launch-profile` (để né `Microsoft.AspNetCore.SpaProxy` hosting-startup-assembly cũ trong `launchSettings.json` trỏ tới thư mục `casemngmt.client` không tồn tại, gây chặn serve static file ở môi trường Development).
- URL hiện tại: **https://unlatch-crucial-tributary.ngrok-free.dev** (đổi mỗi khi restart ngrok, ngrok free tier).
- Sự cố: Windows Defender quarantine binary `ngrok.exe` ngay khi `ngrok update` ghi đè bản mới (báo "contains a virus or potentially unwanted software") — user tự thêm Defender exclusion cho `%LOCALAPPDATA%\Microsoft\WinGet\Packages`, sau đó update+chạy lại bình thường.

### Bug fixes qua feedback trực tiếp từ user
- **Sidebar footer** (`Sidebar.js`): đổi "Powered by ITFreee" → "受注管理システム", bỏ `position:absolute` (nguyên nhân khiến nó nằm lơ lửng giữa menu list thay vì ở cuối).
- **Sidebar resize**: thêm khả năng kéo cạnh phải sidebar để đổi độ rộng (`drawerWidth` chuyển từ const sang state, min 220–max 480, drag handle qua `onMouseDown`+`window mousemove/mouseup`). Rollback 1 lần thử trước đó (giảm font-size menu con) vì gây chữ bị cắt "...", không giải quyết đúng vấn đề.
- **Menu con mặc định collapsed**: `customerOpen`/`caseOpen`/`orderOpen` đổi default `true`→`false`.
- **受注登録 lỗi giả sau khi lưu** (`OrderDetail.js`): `getOrderDetail()` dùng nhầm `orderId` (prop, luôn `undefined` khi tạo mới) thay vì id vừa tạo — gây `GET /api/order?id=undefined` 400 ngay sau khi tạo đơn thành công, hiện banner lỗi giả. Fix: `getOrderDetail(id)` nhận tham số, ưu tiên `id truyền vào → dataId → orderId`; `onSubmit` nhánh tạo mới truyền thẳng `response.data` (id vừa tạo) thay vì dựa vào state chưa kịp cập nhật (stale closure).
- **AI照合 tính sai khi nhiều受注 tranh chấp cùng 1 sản phẩm** (bug do user phát hiện, xác nhận qua đọc code + test API thật): `AiMatchingService` so `item.Quantity` thẳng với `product.StockQuantity`, không trừ đi phần đã bị受注 khác giữ chỗ → 2 đơn khác nhau cùng đặt gần hết tồn kho đều ra 在庫十分 (sai). Fix 2 phần:
  1. `InvoiceService.CreateFromOrderAsync`: trừ thật `Product.StockQuantity` khi xuất請求書 (thời điểm hàng thực sự xuất kho).
  2. `AiMatchingService`: thêm `OrderRepository.GetCommittedQuantitiesAsync` — tính tổng `Quantity` theo `ProductId` từ các受注 khác **chưa Invoiced** (Confirmed/RiskFlagged), trừ vào `StockQuantity` trước khi so sánh; payload gửi Claude thêm `committed_by_other_orders`/`effective_available_stock` để giải thích phản ánh đúng lý do.

### Thêm 検索条件 (search filters) — theo yêu cầu user
- 受注検索: thêm 取引先 (dropdown) + 受注日 (from〜to range, dùng pattern `.section-range` có sẵn trong app). Backend: `IOrderRepository.GetAllAsync` thêm `orderDateFrom`/`orderDateTo`.
- 請求書管理: thêm 取引先 + ステータス (発行済み/入金済み) + 発行日 (range). Backend: `IInvoiceRepository.GetAllAsync` thêm `customerId`/`status`/`issueDateFrom`/`issueDateTo`.
- Thêm cột **AI照合** vào 受注検索 (chip màu 在庫十分/要注意/不足) — trước đó danh sách chỉ có ステータス, không phân biệt được đơn nào đang 要注意 (vì Status chỉ đổi RiskFlagged khi có dòng 不足). Backend: `OrderViewModel` thêm `RiskLevel`, `OrderRiskRepository.GetOverallRiskLevelsByOrderIdsAsync` lấy mức risk tệ nhất theo từng đơn, `OrderService.GetAllOrdersAsync` gán vào kết quả trả về.
- Đổi tên tab trình duyệt: `frontend/public/index.html` — "ITフリーリソース管理" → "受注管理システム".

## Quyết định quan trọng & lý do

- **Repo demo tách biệt, history mới hoàn toàn** thay vì push full history sang remote thứ 2 — vì repo `case-management` cũ đã có AWS key thật trong lịch sử commit, push nguyên history sẽ lộ lại lần nữa. Chỉ sanitize bản copy, không đụng repo gốc (rotate AWS key thật vẫn là việc treo riêng, chưa làm).
- **Trừ tồn kho lúc Invoiced, không phải lúc Confirmed** — bàn trực tiếp với user: `Product.StockQuantity` phải thực sự thay đổi theo thời gian (không thể đứng yên mãi như thiết kế ban đầu tôi đề xuất), nhưng thời điểm hợp lý nhất để coi hàng "đã xuất kho thật" là lúc phát hành hoá đơn — trước đó (Confirmed/RiskFlagged) chỉ là "giữ chỗ", tính động chứ không trừ thật, để đơn còn có thể sửa/chưa chắc chắn mà không cần logic hoàn kho phức tạp.
- **Không xây logic hoàn kho khi huỷ đơn/hoá đơn** — vì hiện UI hoàn toàn chưa có thao tác nào chuyển đơn sang `Cancelled` hay huỷ請求書, xây thêm sẽ là code chết, để dành khi nào có action huỷ thật.
- **`.env.production.local` thay vì set env qua PowerShell `$env:`** để build frontend với `REACT_APP_BASE_URL` rỗng — vì Windows/cmd.exe tự động xoá biến môi trường rỗng khi truyền cho tiến trình con (`npm run build` chạy qua `cmd.exe`), khiến giá trị thành `undefined` thay vì `""`, phải dùng file `.env` (dotenv parse đúng chuỗi rỗng) để né quirk này.
- **`--no-launch-profile` khi chạy backend cho demo/tunnel** thay vì sửa `launchSettings.json` — vì đó là cấu hình dev-loop chia sẻ, không muốn đổi hành vi `npm start`-based dev workflow thông thường chỉ để phục vụ 1 lần chạy demo qua tunnel.

## Vấn đề còn tồn đọng / blocked

- **AWS S3 access key + secret key** vẫn hardcode plaintext trong `backend/CaseMngmt.Server/appsettings.json` gốc (repo `case-management`) — biết từ nhiều phiên trước, chưa rotate, cần user xác nhận vì ảnh hưởng môi trường đang deploy.
- **`.gitignore` chưa có pattern `*.log`** — 2 file `ngrok_run.log` và `backend_run.log`/`backend_run.log.err` vẫn nằm trong working tree, có thể bị `git add .` nhầm nếu không để ý (đã nhắc user ở phiên trước, vẫn chưa xử lý).
- **ngrok URL không cố định** — đổi mỗi khi restart tiến trình ngrok (free tier), cần cập nhật lại cho người dùng cuối mỗi lần.
- **RAG** (hướng mở rộng AI thứ 3 từng thảo luận, sau Dashboard comment + Chat AI) — vẫn chưa triển khai, chưa có quyết định có làm tiếp hay không.
- **Chưa làm**: Excel import cho Product (ClosedXML), chart đẹp (`@mui/x-charts`), nâng cấp OrderNumber/InvoiceNumber sang sequence table atomic.

## Next steps

1. Nếu tiếp tục demo cho khách/bạn: nhớ restart ngrok + lấy URL mới, gửi lại kèm tài khoản đăng nhập (không gửi chung 1 chỗ).
2. User tự commit + push code hiện tại (frontend/backend) lên repo `case-management` gốc qua git client riêng (Claude không thao tác git được trong môi trường này) — rất nhiều thay đổi của phiên này (2026-07-25 và 2026-07-26) chưa được lưu vào version control.
3. Cân nhắc thêm `*.log` vào `.gitignore` gốc trước khi commit lần tới.
4. Rotate AWS S3 key khi có dịp — cần xác nhận trước vì ảnh hưởng deploy hiện tại.
5. Quyết định hướng tiếp theo: RAG, hay các việc treo khác (Excel import, chart, sequence table).
