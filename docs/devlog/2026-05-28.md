# Devlog — 2026-05-28

## Đã làm xong

### Backend

| Phase | Nội dung |
|---|---|
| B1 | Keyword entity + migration (`IsHidden`, `OptionsList`, `Order`) — thực hiện buổi trước |
| B2 | `KeywordController` CRUD đầy đủ: GET (for builder), POST, PUT, DELETE (soft-hide + 409 guard), PATCH reorder |
| B2 | `IKeywordRepository` + `KeywordRepository`: `GetByTemplateIdForBuilderAsync`, `HasCaseKeywordsAsync`, `SoftHideAsync`, `ReorderAsync` |
| B2 | `IKeywordService` + `KeywordService`: `GetByTemplateIdForBuilderAsync`, `SoftDeleteAsync` (trả -1 nếu có CaseKeyword), `ReorderAsync` |
| B3 | `TemplateService.CloneToCompanyAsync` — clone template + keywords + tạo CompanyTemplate |
| B3 | `TemplateService.GetDefaultTemplateAsync` + `TemplateRepository.GetDefaultTemplateAsync` |
| B3 | `CompanyController.Create` tự động clone Standard Template khi tạo company mới |
| B3 | `TemplateController` thêm endpoint `POST /api/template/{id}/clone` |
| B3 | `KeywordRequest` thêm `TemplateId`, `OptionsList`, `IsHidden` |
| B3 | `KeywordViewModel` thêm `IsHidden`, `OptionsList` |

### Frontend

| Phase | Nội dung |
|---|---|
| F1 | `frontend/src/services/templateService.js` — getAll, getById, create, deleteById |
| F1 | `frontend/src/pages/admin/TemplateList.jsx` — danh sách template, dialog tạo mới, navigate to KeywordBuilder |
| F1 | `frontend/src/components/Admin.js` — thêm button "テンプレート管理" |
| F1 | `frontend/src/App.js` — thêm routes `/admin/templates` và `/admin/templates/:templateId/keywords` |
| F2 | `frontend/src/services/keywordService.js` — getByTemplate, create, update, softDelete, reorder |
| F2 | `frontend/src/services/typeService.js` — getAll |
| F2 | `frontend/src/pages/admin/KeywordBuilder.jsx` — CRUD keywords, parallel load, 409 handling |
| F3 | Drag-and-drop reorder trong KeywordBuilder dùng `@dnd-kit/core` + `@dnd-kit/sortable` |
| F3 | `SortableRow` component, optimistic update, revert on error |
| F3 | `frontend/package.json` thêm 3 @dnd-kit packages |

### Tài liệu

- `CLAUDE.md` — kiến trúc, conventions, ràng buộc toàn project
- `.claude/agents/code-reviewer.md` — subagent review
- `.claude/agents/code-tester.md` — subagent test
- `docs/ARCHITECTURE.md` — sơ đồ kiến trúc chi tiết
- `docs/adr/001-004` — 4 Architecture Decision Records

### Buổi chiều (session 2)

| Việc | Nội dung |
|---|---|
| npm install | Cài `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` trong `frontend/` |
| Fix TemplateList create — Hướng B | `TemplateRequest`: thêm `Name?`, bỏ `[Required]` |
| | `ITemplateService`: `AddAsync` → `Task<Guid?>` |
| | `TemplateService.AddAsync`: dùng `request.Name`, cho phép empty keywords, tạo `CompanyTemplate` link, trả `template.Id` |
| | `TemplateController.Create`: bỏ guard, lấy CompanyId từ JWT, trả `Ok(templateId)` |
| | `TemplateList.jsx`: navigate đến `/admin/templates/${response.data}/keywords` sau create |
| Tài liệu | `docs/adr/005-template-create-returns-id.md` |
| | `docs/ARCHITECTURE.md` — thêm section Template Create Pattern |

---

## Đang dở dang

Không còn item dở dang từ plan ban đầu (B1–B3, F1–F3 đều hoàn thành).

---

## Bước tiếp theo cho buổi sau

1. **Test end-to-end toàn bộ flow:**
   - Tạo template mới → tự động redirect KeywordBuilder ✓
   - Thêm keyword (các loại: text, number, date, list với OptionsList) ✓
   - Drag reorder ✓
   - Ẩn/hiện keyword (bao gồm case 409 khi keyword đang dùng) ✓
   - Tạo Company mới → verify Standard Template được auto-clone ✓

2. **Verify API response shape:** `GET /api/template/getAll` — kiểm tra `response.data?.items` có đúng không khi TemplateList load danh sách

3. **Tính năng tiếp theo** (nếu cần): theo yêu cầu của user

---

## Blocker / Việc cần làm thủ công

| Việc | Người thực hiện | Ghi chú |
|---|---|---|
| Verify API response shape `getAll` | Test thực tế | `response.data?.items` vs `response.data` |

---

## Không có regression

- `CaseSearch.jsx` — dùng `/api/Case/template`, không bị ảnh hưởng
- `CaseDetail.js` — dùng `/api/Template/template`, trả `KeywordViewModel` với `isHidden`/`optionsList` là fields mới (additive, code cũ ignore)
